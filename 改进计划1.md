# BikeCare Pro 改进计划

> **版本**: 1.1 (修订版)
> **创建日期**: 2026-03-14
> **修订日期**: 2026-03-14
> **目标**: 修复现有问题，完善核心功能，提升用户体验

---

## 目录

1. [改进原则](#一改进原则)
2. [P0 紧急修复](#二p0-紧急修复)
3. [P1 短期改进](#三p1-短期改进)
4. [P2 中期规划](#四p2-中期规划)
5. [文件结构规范](#五文件结构规范)
6. [代码规范](#六代码规范)
7. [测试标准](#七测试标准)
8. [实施计划](#八实施计划)

---

## 一、改进原则

### 1.1 功能模块原则

```
┌─────────────────────────────────────────────────────────────────┐
│                      功能模块设计原则                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ✅ 单一职责：一个文件/类只负责一个功能                           │
│  ✅ 高内聚低耦合：模块内部紧密关联，模块间依赖最小化               │
│  ✅ 命名清晰：文件名、类名、方法名语义化，见名知意                 │
│  ✅ 文件结构语义化：目录结构反映功能层次                          │
│                                                                 │
│  示例：                                                         │
│  Features/                                                      │
│  ├── Analytics/                                                 │
│  │   ├── CostAnalyticsView.swift      # 成本分析视图             │
│  │   └── ViewModels/                                            │
│  │       └── AnalyticsViewModel.swift # 分析视图模型             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 代码复用原则

```
┌─────────────────────────────────────────────────────────────────┐
│                      代码复用原则                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ✅ 优先复用现有模块                                             │
│  ✅ 遵循"三次法则"：遇到三次重复再抽象                            │
│  ✅ 合并相似代码，避免重复实现                                    │
│  ✅ 提取公共组件到 Shared/Components/                            │
│                                                                 │
│  当前代码复用检查（重要）：                                      │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ 已存在可复用：                                           │    │
│  │ - CostAnalyzer.swift (Core/Domain/Services/)            │    │
│  │ - TimeRange, SpendingTrend (已定义在 CostAnalyzer.swift)│    │
│  │ - Constants.Storage.onboardingCompleted                 │    │
│  │ - PersistenceController.shared                          │    │
│  │                                                         │    │
│  │ ⚠️ 注意：不要重复定义这些内容！                          │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.3 重构清理原则

```
┌─────────────────────────────────────────────────────────────────┐
│                      重构清理原则                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  步骤：                                                         │
│  1. 标记废弃：使用 @available(*, deprecated) 标记               │
│  2. 验证无影响：编译测试确保无引用                                │
│  3. 删除代码：清理废弃代码和文件                                  │
│  4. 提交说明：Git commit 注明清理范围                            │
│                                                                 │
│  当前需要清理的重复代码：                                        │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ AnalyticsViewModel.swift 中的重复定义：                  │    │
│  │ - SpendingTrend (已在 CostAnalyzer.swift 定义)          │    │
│  │ - TimeRange (已在 CostAnalyzer.swift 定义)              │    │
│  │ 解决方案：删除重复定义，统一使用 CostAnalyzer 中的定义    │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.4 风格一致性原则

```
┌─────────────────────────────────────────────────────────────────┐
│                      风格一致性原则                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ✅ UI 风格：使用现有 Color+Theme.swift 配色方案                 │
│  ✅ 交互风格：保持与现有页面一致的交互模式                        │
│  ✅ 命名风格：遵循现有命名规范                                    │
│  ✅ 注释风格：使用现有注释格式                                    │
│                                                                 │
│  现有风格参考：                                                  │
│  - 颜色：Color.theme.primary, Color.theme.color(for:)           │
│  - 圆角：Constants.UI.cornerRadius (12pt)                       │
│  - 间距：Constants.UI.defaultPadding (16pt)                     │
│  - 字体：.headline, .subheadline, .caption                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.5 继承兼容性原则

```
┌─────────────────────────────────────────────────────────────────┐
│                      继承兼容性原则                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ✅ 优先继承现有设置和配置                                        │
│  ✅ 使用 @AppStorage 读取现有用户偏好                             │
│  ✅ 复用 Constants.swift 中的常量                                │
│  ✅ 避免硬编码，使用配置文件                                      │
│                                                                 │
│  当前代码中的常量（必须使用）：                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ Constants.Storage.onboardingCompleted                   │    │
│  │ Constants.Storage.preferredDistanceUnit                 │    │
│  │ Constants.Storage.notificationsEnabled                  │    │
│  │ Constants.UI.cornerRadius                               │    │
│  │ Constants.UI.defaultPadding                             │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、P0 紧急修复

### 2.1 修复 CostAnalyticsView 数据连接

#### 2.1.1 需求描述

**问题**：CostAnalyticsView 中的 AnalyticsViewModel 未连接实际数据源，导致成本分析页面显示所有数据为 0 或空。

**影响**：用户无法查看成本分析数据，核心功能不可用。

**当前代码分析**：

```
┌─────────────────────────────────────────────────────────────────┐
│                      当前代码状态分析                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  已存在的文件：                                                  │
│  1. CostAnalyzer.swift (Core/Domain/Services/)                  │
│     - 已定义 SpendingTrend, TimeRange                           │
│     - 已实现 analyzeSpending() 方法                              │
│     - 需要 ModelContext 初始化                                   │
│                                                                 │
│  2. AnalyticsViewModel.swift (Features/Analytics/ViewModels/)   │
│     - ⚠️ 重复定义了 SpendingTrend, TimeRange                    │
│     - analyze() 方法需要传入 CostEntry 数组                      │
│                                                                 │
│  3. CostAnalyticsView.swift                                     │
│     - 使用 @StateObject private var viewModel: AnalyticsViewModel│
│     - ❌ 未调用 viewModel.analyze()                              │
│     - 有 @Query private var costEntries: [CostEntry]            │
│                                                                 │
│  问题诊断：                                                      │
│  ❌ AnalyticsViewModel 与 CostAnalyzer 功能重复                  │
│  ❌ CostAnalyticsView 未调用 analyze()                           │
│  ❌ SpendingTrend, TimeRange 定义重复（违反 DRY 原则）            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 2.1.2 生成规则

```
┌─────────────────────────────────────────────────────────────────┐
│                      生成规则                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  方案选择：复用现有 CostAnalyzer（推荐）                          │
│                                                                 │
│  理由：                                                         │
│  1. CostAnalyzer 已完整实现分析逻辑                              │
│  2. CostAnalyzer 支持 ModelContext 直接查询                      │
│  3. 避免代码重复，符合 DRY 原则                                   │
│  4. SpendingTrend, TimeRange 已在 CostAnalyzer.swift 定义       │
│                                                                 │
│  实施步骤：                                                      │
│  1. 修改 CostAnalyticsView 使用 CostAnalyzer                    │
│  2. 在 onAppear 中调用 analyzeSpending()                        │
│  3. 删除 AnalyticsViewModel 中的重复定义                         │
│  4. 统一使用 CostAnalyzer.swift 中的 TimeRange, SpendingTrend   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 2.1.3 代码编写示例

**修改文件**: `Features/Analytics/CostAnalyticsView.swift`

```swift
import SwiftUI
import SwiftData
import Charts

struct CostAnalyticsView: View {
    @Environment(\.modelContext) private var modelContext
    
    @Query(sort: \Bicycle.name) private var bicycles: [Bicycle]
    
    @State private var selectedTimeRange: TimeRange = .allTime
    @State private var selectedBike: Bicycle?
    
    // 使用 CostAnalyzer 而非 AnalyticsViewModel
    @StateObject private var costAnalyzer: CostAnalyzer
    
    init() {
        // CostAnalyzer 需要 ModelContext，但 init 时还无法获取
        // 使用临时占位，在 onAppear 中重新初始化
        let tempContainer = try! ModelContainer(for: Bicycle.self)
        _costAnalyzer = StateObject(wrappedValue: CostAnalyzer(modelContext: tempContainer.mainContext))
    }
    
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(spacing: Constants.UI.defaultPadding) {
                    timeRangePicker
                    totalSpendingCard
                    spendingByCategorySection
                    spendingByBikeSection
                    monthlyTrendSection
                }
                .padding()
            }
            .navigationTitle("Cost Analytics")
        }
        .onAppear {
            // 重新初始化 CostAnalyzer 以获取正确的 modelContext
            costAnalyzer = CostAnalyzer(modelContext: modelContext)
            Task {
                await costAnalyzer.analyzeSpending(for: selectedBike, timeRange: selectedTimeRange)
            }
        }
        .onChange(of: selectedTimeRange) { _, newRange in
            Task {
                await costAnalyzer.analyzeSpending(for: selectedBike, timeRange: newRange)
            }
        }
        .onChange(of: selectedBike) { _, newBike in
            Task {
                await costAnalyzer.analyzeSpending(for: newBike, timeRange: selectedTimeRange)
            }
        }
    }
    
    private var timeRangePicker: some View {
        Picker("Time Range", selection: $selectedTimeRange) {
            ForEach(TimeRange.allCases, id: \.self) { range in
                Text(range.rawValue).tag(range)
            }
        }
        .pickerStyle(.segmented)
    }
    
    private var totalSpendingCard: some View {
        VStack(spacing: 12) {
            Text("Total Spending")
                .font(.subheadline)
                .foregroundColor(.secondary)
            
            Text(costAnalyzer.totalSpending.formattedCurrency)
                .font(.system(size: 36, weight: .bold))
            
            let trend = costAnalyzer.getSpendingTrend()
            HStack {
                Image(systemName: trend.icon)
                Text(trend.description)
            }
            .font(.caption)
            .foregroundColor(trend == .increasing ? .red : .green)
        }
        .frame(maxWidth: .infinity)
        .padding()
        .background(Color(.systemGray6))
        .cornerRadius(Constants.UI.cornerRadius)
    }
    
    private var spendingByCategorySection: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("Spending by Category")
                .font(.headline)
            
            if costAnalyzer.categorySpending.isEmpty {
                Text("No data available")
                    .foregroundColor(.secondary)
            } else {
                Chart(costAnalyzer.categorySpending.sorted(by: { $0.value > $1.value }), id: \.key) { item in
                    BarMark(
                        x: .value("Amount", item.value),
                        y: .value("Category", item.key.rawValue)
                    )
                    .foregroundStyle(Color.theme.primary)
                    .annotation(position: .trailing) {
                        Text(item.value.formattedCurrency)
                            .font(.caption)
                    }
                }
                .chartXAxis(.hidden)
                .frame(height: CGFloat(costAnalyzer.categorySpending.count) * 40)
            }
        }
        .padding()
        .background(Color(.systemGray6))
        .cornerRadius(Constants.UI.cornerRadius)
    }
    
    private var spendingByBikeSection: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("Spending by Bike")
                .font(.headline)
            
            if bicycles.isEmpty {
                Text("No bikes added")
                    .foregroundColor(.secondary)
            } else {
                ForEach(bicycles) { bike in
                    HStack {
                        VStack(alignment: .leading, spacing: 2) {
                            Text(bike.name)
                                .font(.subheadline)
                            Text(bike.totalDistance.formattedDistance + " total")
                                .font(.caption)
                                .foregroundColor(.secondary)
                        }
                        
                        Spacer()
                        
                        VStack(alignment: .trailing, spacing: 2) {
                            Text(bike.totalMaintenanceCost.formattedCurrency)
                                .font(.subheadline)
                                .fontWeight(.medium)
                            Text(costAnalyzer.getCostPerKilometer(for: bike).formattedCurrency + "/km")
                                .font(.caption2)
                                .foregroundColor(.secondary)
                        }
                    }
                    .padding(.vertical, 4)
                    
                    if bike.id != bicycles.last?.id {
                        Divider()
                    }
                }
            }
        }
        .padding()
        .background(Color(.systemGray6))
        .cornerRadius(Constants.UI.cornerRadius)
    }
    
    private var monthlyTrendSection: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("Monthly Trend")
                .font(.headline)
            
            if costAnalyzer.monthlySpending.isEmpty {
                Text("No data available")
                    .foregroundColor(.secondary)
            } else {
                let sortedMonths = costAnalyzer.monthlySpending.keys.sorted()
                Chart(sortedMonths, id: \.self) { month in
                    if let amount = costAnalyzer.monthlySpending[month] {
                        LineMark(
                            x: .value("Month", month),
                            y: .value("Amount", amount)
                        )
                        .foregroundStyle(Color.theme.primary)
                        
                        PointMark(
                            x: .value("Month", month),
                            y: .value("Amount", amount)
                        )
                        .foregroundStyle(Color.theme.primary)
                    }
                }
                .frame(height: 200)
            }
        }
        .padding()
        .background(Color(.systemGray6))
        .cornerRadius(Constants.UI.cornerRadius)
    }
}

#Preview {
    CostAnalyticsView()
        .modelContainer(PersistenceController(inMemory: true).container)
}
```

**清理文件**: `Features/Analytics/ViewModels/AnalyticsViewModel.swift`

```swift
//
//  AnalyticsViewModel.swift
//  BikeCare Pro
//
//  DEPRECATED: This file is deprecated.
//  Use CostAnalyzer (Core/Domain/Services/CostAnalyzer.swift) instead.
//
//  TimeRange and SpendingTrend are now defined in CostAnalyzer.swift
//

// ⚠️ 此文件已废弃，功能已合并到 CostAnalyzer.swift
// 保留此文件仅作为参考，后续可删除

import Foundation

// MARK: - 以下定义已移动到 CostAnalyzer.swift
// TimeRange - 已在 CostAnalyzer.swift 中定义
// SpendingTrend - 已在 CostAnalyzer.swift 中定义

// 如果有其他独特功能，保留在此
// 否则整个文件可以删除
```

#### 2.1.4 测试用例

```swift
import XCTest
@testable import BikeCare

final class CostAnalyzerTests: XCTestCase {
    
    var costAnalyzer: CostAnalyzer!
    var modelContainer: ModelContainer!
    var modelContext: ModelContext!
    
    override func setUp() async throws {
        try await super.setUp()
        
        let schema = Schema([Bicycle.self, Component.self, CostEntry.self])
        let configuration = ModelConfiguration(schema: schema, isStoredInMemoryOnly: true)
        modelContainer = try ModelContainer(for: schema, configurations: configuration)
        modelContext = ModelContext(modelContainer)
        costAnalyzer = CostAnalyzer(modelContext: modelContext)
    }
    
    override func tearDown() {
        costAnalyzer = nil
        modelContext = nil
        modelContainer = nil
        super.tearDown()
    }
    
    // 测试用例 1: 空数据分析
    func testAnalyzeWithEmptyData() async {
        await costAnalyzer.analyzeSpending(timeRange: .allTime)
        
        XCTAssertEqual(costAnalyzer.totalSpending, 0, "空数据时总支出应为 0")
        XCTAssertTrue(costAnalyzer.monthlySpending.isEmpty, "空数据时月度支出应为空")
        XCTAssertTrue(costAnalyzer.categorySpending.isEmpty, "空数据时分类支出应为空")
    }
    
    // 测试用例 2: 单条数据分析
    func testAnalyzeWithSingleEntry() async {
        let entry = CostEntry(title: "Test", costType: .maintenance, amount: 100)
        modelContext.insert(entry)
        
        await costAnalyzer.analyzeSpending(timeRange: .allTime)
        
        XCTAssertEqual(costAnalyzer.totalSpending, 100, "总支出应为 100")
        XCTAssertEqual(costAnalyzer.categorySpending[.maintenance], 100, "维护分类支出应为 100")
    }
    
    // 测试用例 3: 时间范围过滤
    func testAnalyzeWithTimeRangeFilter() async {
        let calendar = Calendar.current
        let now = Date()
        
        let oldEntry = CostEntry(title: "Old", costType: .maintenance, amount: 50)
        oldEntry.date = calendar.date(byAdding: .month, value: -2, to: now)!
        
        let newEntry = CostEntry(title: "New", costType: .maintenance, amount: 100)
        
        modelContext.insert(oldEntry)
        modelContext.insert(newEntry)
        
        await costAnalyzer.analyzeSpending(timeRange: .lastMonth)
        
        XCTAssertEqual(costAnalyzer.totalSpending, 100, "最近一个月只应包含新条目")
    }
    
    // 测试用例 4: 分类汇总
    func testCategorySpendingAggregation() async {
        let entries = [
            CostEntry(title: "M1", costType: .maintenance, amount: 100),
            CostEntry(title: "M2", costType: .maintenance, amount: 50),
            CostEntry(title: "C1", costType: .component, amount: 200)
        ]
        
        for entry in entries {
            modelContext.insert(entry)
        }
        
        await costAnalyzer.analyzeSpending(timeRange: .allTime)
        
        XCTAssertEqual(costAnalyzer.categorySpending[.maintenance], 150, "维护分类应为 150")
        XCTAssertEqual(costAnalyzer.categorySpending[.component], 200, "零件分类应为 200")
        XCTAssertEqual(costAnalyzer.totalSpending, 350, "总支出应为 350")
    }
    
    // 测试用例 5: 趋势计算
    func testSpendingTrendCalculation() async {
        let calendar = Calendar.current
        let now = Date()
        
        for i in 0..<6 {
            let entry = CostEntry(title: "Entry \(i)", costType: .maintenance, amount: Double(i + 1) * 100)
            entry.date = calendar.date(byAdding: .month, value: -(5 - i), to: now)!
            modelContext.insert(entry)
        }
        
        await costAnalyzer.analyzeSpending(timeRange: .allTime)
        let trend = costAnalyzer.getSpendingTrend()
        
        XCTAssertEqual(trend, .increasing, "支出增长趋势应为 increasing")
    }
    
    // 测试用例 6: 按自行车过滤
    func testAnalyzeByBicycle() async {
        let bike1 = Bicycle(name: "Bike1", brand: "Test", model: "Test", year: 2024, bikeType: .road)
        let bike2 = Bicycle(name: "Bike2", brand: "Test", model: "Test", year: 2024, bikeType: .road)
        
        modelContext.insert(bike1)
        modelContext.insert(bike2)
        
        let entry1 = CostEntry(title: "E1", costType: .maintenance, amount: 100)
        entry1.bicycle = bike1
        
        let entry2 = CostEntry(title: "E2", costType: .maintenance, amount: 200)
        entry2.bicycle = bike2
        
        modelContext.insert(entry1)
        modelContext.insert(entry2)
        
        await costAnalyzer.analyzeSpending(for: bike1, timeRange: .allTime)
        
        XCTAssertEqual(costAnalyzer.totalSpending, 100, "Bike1 的支出应为 100")
        
        await costAnalyzer.analyzeSpending(for: bike2, timeRange: .allTime)
        
        XCTAssertEqual(costAnalyzer.totalSpending, 200, "Bike2 的支出应为 200")
    }
}
```

#### 2.1.5 检验标准

| 检验项 | 标准 | 验证方法 |
|--------|------|----------|
| 数据连接 | CostAnalyzer 正确初始化并调用 | 添加断点或日志验证 |
| 时间过滤 | 切换时间范围后数据正确更新 | 手动测试 + 单元测试 |
| 分类汇总 | 各分类金额计算正确 | 单元测试 |
| 趋势显示 | 趋势图标和描述正确显示 | 手动测试 |
| 性能 | 大量数据（1000条）分析 < 1秒 | 性能测试 |
| 代码清理 | AnalyticsViewModel 重复定义已删除 | 代码审查 |

---

### 2.2 添加应用启动时的维护检查

#### 2.2.1 需求描述

**问题**：MaintenanceScheduler 已实现但未在应用启动时调用，导致用户不会收到零件更换提醒。

**影响**：核心提醒功能不可用，用户可能错过重要维护时机。

**当前代码分析**：

```
┌─────────────────────────────────────────────────────────────────┐
│                      当前代码状态分析                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  已存在的文件：                                                  │
│  1. carebikeApp.swift                                           │
│     - 已有 PersistenceController.shared                          │
│     - 已有 .modelContainer()                                     │
│     - 已有 .environmentObject(persistenceController)             │
│                                                                 │
│  2. MaintenanceScheduler.swift (Core/Domain/Services/)          │
│     - 已实现 checkMaintenanceNeeds()                             │
│     - 需要 ModelContext 初始化                                   │
│                                                                 │
│  3. NotificationManager.swift (Core/Domain/Services/)           │
│     - 已实现通知调度功能                                          │
│                                                                 │
│  问题诊断：                                                      │
│  ❌ MaintenanceScheduler 未被初始化                              │
│  ❌ 应用启动时未调用检查方法                                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 2.2.2 生成规则

```
┌─────────────────────────────────────────────────────────────────┐
│                      生成规则                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  方案选择：扩展现有 PersistenceController                        │
│                                                                 │
│  理由：                                                         │
│  1. PersistenceController 已作为 @StateObject 存在              │
│  2. 避免创建新的 AppState，减少复杂度                            │
│  3. 复用现有架构，保持一致性                                      │
│                                                                 │
│  实施步骤：                                                      │
│  1. 在 PersistenceController 中添加 MaintenanceScheduler        │
│  2. 添加 checkMaintenanceNeeds() 方法                            │
│  3. 在 ContentView.onAppear 中调用检查                           │
│  4. 使用 NotificationCenter 监听应用前台事件                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 2.2.3 代码编写示例

**修改文件**: `Core/Data/Persistence/PersistenceController.swift`

```swift
import SwiftUI
import SwiftData

@MainActor
class PersistenceController: ObservableObject {
    static let shared = PersistenceController()
    
    let container: ModelContainer
    private var maintenanceScheduler: MaintenanceScheduler?
    
    @Published var overdueComponents: [Component] = []
    @Published var upcomingMaintenance: [MaintenanceEvent] = []
    
    init(inMemory: Bool = false) {
        do {
            let schema = Schema([
                Bicycle.self,
                Component.self,
                MaintenanceEvent.self,
                Ride.self,
                CostEntry.self
            ])
            
            let modelConfiguration: ModelConfiguration
            if inMemory {
                modelConfiguration = ModelConfiguration(
                    schema: schema,
                    isStoredInMemoryOnly: true,
                    allowsSave: true
                )
            } else {
                modelConfiguration = ModelConfiguration(
                    schema: schema,
                    isStoredInMemoryOnly: false,
                    allowsSave: true
                )
            }
            
            container = try ModelContainer(
                for: schema,
                configurations: modelConfiguration
            )
            
            // 初始化维护调度器
            maintenanceScheduler = MaintenanceScheduler(modelContext: container.mainContext)
            
        } catch {
            fatalError("Failed to initialize ModelContainer: \(error)")
        }
    }
    
    // MARK: - Maintenance Check
    
    func checkMaintenanceNeeds() async {
        await maintenanceScheduler?.checkMaintenanceNeeds()
        
        // 更新发布属性供 UI 使用
        if let scheduler = maintenanceScheduler {
            overdueComponents = scheduler.overdueComponents
            upcomingMaintenance = scheduler.upcomingMaintenance
        }
    }
    
    func scheduleMaintenance(for bicycle: Bicycle, type: MaintenanceType, date: Date, notes: String? = nil) async throws -> MaintenanceEvent? {
        return try await maintenanceScheduler?.scheduleMaintenance(for: bicycle, type: type, date: date, notes: notes)
    }
    
    func completeMaintenance(_ event: MaintenanceEvent) {
        maintenanceScheduler?.completeMaintenance(event)
    }
}
```

**修改文件**: `carebikeApp.swift`

```swift
import SwiftUI
import SwiftData

@main
struct carebikeApp: App {
    @StateObject private var persistenceController = PersistenceController.shared
    
    var body: some Scene {
        WindowGroup {
            ContentView()
                .modelContainer(persistenceController.container)
                .environmentObject(persistenceController)
                .onReceive(NotificationCenter.default.publisher(for: UIApplication.didBecomeActiveNotification)) { _ in
                    Task {
                        await persistenceController.checkMaintenanceNeeds()
                    }
                }
        }
    }
}
```

**修改文件**: `ContentView.swift`

```swift
import SwiftUI
import SwiftData

struct ContentView: View {
    @EnvironmentObject var persistenceController: PersistenceController
    @State private var selectedTab: Tab = .dashboard
    
    // ... 其他代码保持不变
    
    var body: some View {
        TabView(selection: $selectedTab) {
            // ... TabView 内容保持不变
        }
        .tint(.blue)
        .onAppear {
            Task {
                await persistenceController.checkMaintenanceNeeds()
            }
        }
    }
}

// ... StartRideView, MoreView 保持不变
```

struct ContentView: View {
    @Environment(\.modelContext) private var modelContext
    @EnvironmentObject var appState: AppState
    
    @State private var selectedTab: Tab = .dashboard
    
    // ... 其他代码保持不变
    
    var body: some View {
        TabView(selection: $selectedTab) {
            // ... TabView 内容
        }
        .onAppear {
            appState.setupMaintenanceScheduler(modelContext: modelContext)
            Task {
                await appState.checkMaintenanceNeeds()
            }
        }
    }
}
```

#### 2.2.4 测试用例

```swift
import XCTest
@testable import BikeCare

final class MaintenanceSchedulerTests: XCTestCase {
    
    var scheduler: MaintenanceScheduler!
    var modelContainer: ModelContainer!
    var modelContext: ModelContext!
    
    override func setUp() async throws {
        try await super.setUp()
        
        let schema = Schema([
            Bicycle.self,
            Component.self,
            MaintenanceEvent.self
        ])
        
        let configuration = ModelConfiguration(
            schema: schema,
            isStoredInMemoryOnly: true
        )
        
        modelContainer = try ModelContainer(for: schema, configurations: configuration)
        modelContext = ModelContext(modelContainer)
        scheduler = MaintenanceScheduler(modelContext: modelContext)
    }
    
    override func tearDown() {
        scheduler = nil
        modelContext = nil
        modelContainer = nil
        super.tearDown()
    }
    
    // 测试用例 1: 检查零件磨损提醒
    func testCheckComponentWear() async throws {
        let bike = Bicycle(name: "Test", brand: "Test", model: "Test", year: 2024, bikeType: .road)
        let component = Component(name: "Chain", componentType: .chain)
        component.currentDistance = 2000 // 达到更换里程
        component.bicycle = bike
        
        modelContext.insert(bike)
        modelContext.insert(component)
        
        await scheduler.checkMaintenanceNeeds()
        
        XCTAssertTrue(scheduler.overdueComponents.contains(where: { $0.id == component.id }), 
                      "磨损零件应被标记为需要更换")
    }
    
    // 测试用例 2: 检查计划维护
    func testCheckScheduledMaintenance() async throws {
        let bike = Bicycle(name: "Test", brand: "Test", model: "Test", year: 2024, bikeType: .road)
        let event = MaintenanceEvent(title: "Test Maintenance", maintenanceType: .routine)
        event.isScheduled = true
        event.scheduledDate = Date()
        event.bicycle = bike
        
        modelContext.insert(bike)
        modelContext.insert(event)
        
        await scheduler.checkMaintenanceNeeds()
        
        XCTAssertTrue(scheduler.upcomingMaintenance.contains(where: { $0.id == event.id }),
                      "计划维护应出现在即将到来的维护列表中")
    }
    
    // 测试用例 3: 完成维护后更新状态
    func testCompleteMaintenance() {
        let bike = Bicycle(name: "Test", brand: "Test", model: "Test", year: 2024, bikeType: .road)
        let event = MaintenanceEvent(title: "Test", maintenanceType: .routine)
        event.bicycle = bike
        
        modelContext.insert(bike)
        modelContext.insert(event)
        
        scheduler.completeMaintenance(event)
        
        XCTAssertTrue(event.isCompleted, "维护事件应标记为已完成")
        XCTAssertNotNil(bike.lastMaintenanceDate, "自行车最后维护日期应更新")
    }
}
```

#### 2.2.5 检验标准

| 检验项 | 标准 | 验证方法 |
|--------|------|----------|
| 启动检查 | 应用启动时自动执行维护检查 | 添加日志验证 |
| 前台检查 | 应用进入前台时重新检查 | 手动测试 |
| 通知权限 | 首次检查时请求通知权限 | 手动测试 |
| 零件提醒 | 磨损达 80% 显示警告 | 创建测试数据验证 |
| 零件更换 | 磨损达 100% 显示紧急提醒 | 创建测试数据验证 |

---

### 2.3 修复 Dashboard SF Symbol 兼容性

#### 2.3.1 需求描述

**问题**：DashboardView 中使用 `figure.cycling` SF Symbol，可能在某些 iOS 版本不可用。

**影响**：图标显示为空白或问号。

#### 2.3.2 生成规则

```
┌─────────────────────────────────────────────────────────────────┐
│                      生成规则                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 将 figure.cycling 替换为 bicycle.circle.fill               │
│  2. 保持与 ContentView 中 Track Tab 图标一致                     │
│  3. 检查所有使用 figure.cycling 的地方并统一替换                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 2.3.3 代码编写示例

**文件**: `Features/Dashboard/DashboardView.swift`

```swift
// 修改前
SummaryCard(
    title: "Total Distance",
    value: totalDistance.formattedDistance,
    icon: "figure.cycling",  // ❌ 不兼容
    color: .blue
)

// 修改后
SummaryCard(
    title: "Total Distance",
    value: totalDistance.formattedDistance,
    icon: "bicycle.circle.fill",  // ✅ 兼容
    color: .blue
)
```

**需要修改的文件列表**:

| 文件 | 行号 | 原图标 | 新图标 |
|------|------|--------|--------|
| DashboardView.swift | 49 | figure.cycling | bicycle.circle.fill |
| DashboardView.swift | 143 | figure.cycling | bicycle |
| DashboardView.swift | 180 | figure.cycling | bicycle |
| BikeListView.swift | 75 | figure.cycling | bicycle |
| EmptyStateView.swift | 94 | figure.cycling | bicycle |
| RideHistoryView.swift | 89 | figure.cycling | bicycle |

#### 2.3.4 测试用例

```swift
import XCTest
@testable import BikeCare

final class SFSymbolTests: XCTestCase {
    
    // 测试用例: 验证所有 SF Symbol 可用
    func testAllSFSymbolsAreValid() {
        let symbols = [
            "house.fill",
            "bicycle",
            "bicycle.circle.fill",
            "wrench.and.screwdriver.fill",
            "ellipsis.circle.fill",
            "gearshape.fill",
            "chart.bar.fill",
            "map.fill",
            "book.fill",
            "play.fill",
            "stop.fill",
            "pause.fill"
        ]
        
        for symbol in symbols {
            XCTAssertNotNil(UIImage(systemName: symbol), 
                           "SF Symbol '\(symbol)' should be available")
        }
    }
}
```

#### 2.3.5 检验标准

| 检验项 | 标准 | 验证方法 |
|--------|------|----------|
| 图标显示 | 所有图标正常显示 | 手动测试 |
| 编译通过 | 无编译警告 | xcodebuild |
| 视觉一致 | 图标风格统一 | 手动检查 |

---

## 三、P1 短期改进

### 3.1 添加 Onboarding 引导流程

#### 3.1.1 需求描述

**问题**：首次使用时缺少引导流程，用户不知道如何开始使用。

**影响**：新用户可能放弃使用，影响留存率。

**当前代码分析**：

```
┌─────────────────────────────────────────────────────────────────┐
│                      当前代码状态分析                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  已存在的常量：                                                  │
│  Constants.Storage.onboardingCompleted = "onboarding_completed" │
│                                                                 │
│  ⚠️ 必须使用此常量，不要硬编码字符串！                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.1.2 生成规则

```
┌─────────────────────────────────────────────────────────────────┐
│                      生成规则                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 创建 OnboardingView.swift 文件                               │
│  2. 使用 TabView 实现滑动引导页                                  │
│  3. 包含 3-4 页引导内容                                          │
│  4. 使用 Constants.Storage.onboardingCompleted 记录状态         │
│  5. 引导完成后跳转到主页面                                        │
│  6. 使用 Constants.UI 中的常量定义间距和圆角                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.1.3 文件结构

```
Features/
└── Onboarding/
    ├── OnboardingView.swift           # 引导主视图
    └── OnboardingPageView.swift       # 单页引导组件
```

#### 3.1.4 代码编写示例

**文件**: `Features/Onboarding/OnboardingView.swift`

```swift
import SwiftUI

struct OnboardingView: View {
    // ✅ 使用 Constants 中的常量，不要硬编码
    @AppStorage(Constants.Storage.onboardingCompleted) private var hasCompletedOnboarding = false
    @State private var currentPage = 0
    
    private let pages: [OnboardingPage] = [
        OnboardingPage(
            title: "Welcome to BikeCare Pro",
            subtitle: "Your personal bike maintenance assistant",
            iconName: "bicycle.circle.fill",
            gradientColors: [Color.blue, Color.purple]
        ),
        OnboardingPage(
            title: "Track Your Rides",
            subtitle: "GPS tracking automatically updates component mileage",
            iconName: "map.fill",
            gradientColors: [Color.green, Color.blue]
        ),
        OnboardingPage(
            title: "Smart Reminders",
            subtitle: "Never miss maintenance with intelligent alerts",
            iconName: "bell.badge.fill",
            gradientColors: [Color.orange, Color.red]
        ),
        OnboardingPage(
            title: "Get Started",
            subtitle: "Add your first bike to begin tracking",
            iconName: "plus.circle.fill",
            gradientColors: [Color.purple, Color.blue]
        )
    ]
    
    var body: some View {
        VStack(spacing: 0) {
            TabView(selection: $currentPage) {
                ForEach(0..<pages.count, id: \.self) { index in
                    OnboardingPageView(page: pages[index])
                        .tag(index)
                }
            }
            .tabViewStyle(.page(indexDisplayMode: .automatic))
            
            bottomButtons
        }
        .background(Color(.systemBackground))
    }
    
    private var bottomButtons: some View {
        HStack(spacing: Constants.UI.defaultPadding) {
            if currentPage < pages.count - 1 {
                Button("Skip") {
                    completeOnboarding()
                }
                .foregroundColor(.secondary)
                
                Spacer()
                
                Button("Next") {
                    withAnimation {
                        currentPage += 1
                    }
                }
                .fontWeight(.semibold)
                .padding(.horizontal, Constants.UI.defaultPadding)
                .padding(.vertical, Constants.UI.smallPadding)
                .background(Color.blue)
                .foregroundColor(.white)
                .cornerRadius(Constants.UI.cornerRadius)
            } else {
                Button(action: completeOnboarding) {
                    Text("Get Started")
                        .fontWeight(.semibold)
                        .frame(maxWidth: .infinity)
                        .padding(.vertical, Constants.UI.defaultPadding)
                        .background(Color.blue)
                        .foregroundColor(.white)
                        .cornerRadius(Constants.UI.cornerRadius)
                }
            }
        }
        .padding(Constants.UI.defaultPadding)
    }
    
    private func completeOnboarding() {
        hasCompletedOnboarding = true
    }
}

struct OnboardingPage {
    let title: String
    let subtitle: String
    let iconName: String
    let gradientColors: [Color]
}
```

**文件**: `Features/Onboarding/OnboardingPageView.swift`

```swift
import SwiftUI

struct OnboardingPageView: View {
    let page: OnboardingPage
    
    var body: some View {
        VStack(spacing: 32) {
            Spacer()
            
            iconView
            
            VStack(spacing: 12) {
                Text(page.title)
                    .font(.title)
                    .fontWeight(.bold)
                    .multilineTextAlignment(.center)
                
                Text(page.subtitle)
                    .font(.body)
                    .foregroundColor(.secondary)
                    .multilineTextAlignment(.center)
                    .padding(.horizontal, 40)
            }
            
            Spacer()
            Spacer()
        }
    }
    
    private var iconView: some View {
        ZStack {
            Circle()
                .fill(
                    LinearGradient(
                        colors: page.gradientColors,
                        startPoint: .topLeading,
                        endPoint: .bottomTrailing
                    )
                )
                .frame(width: 150, height: 150)
            
            Image(systemName: page.iconName)
                .font(.system(size: 60))
                .foregroundColor(.white)
        }
    }
}
```

**修改文件**: `ContentView.swift`

```swift
struct ContentView: View {
    // ✅ 使用 Constants 中的常量
    @AppStorage(Constants.Storage.onboardingCompleted) private var hasCompletedOnboarding = false
    @EnvironmentObject var persistenceController: PersistenceController
    @State private var selectedTab: Tab = .dashboard
    
    var body: some View {
        if hasCompletedOnboarding {
            mainTabView
        } else {
            OnboardingView()
        }
    }
    
    @ViewBuilder
    private var mainTabView: some View {
        TabView(selection: $selectedTab) {
            // ... 现有 TabView 内容
        }
        .tint(.blue)
        .onAppear {
            Task {
                await persistenceController.checkMaintenanceNeeds()
            }
        }
    }
}
```

#### 3.1.5 测试用例

```swift
import XCTest
@testable import BikeCare

final class OnboardingTests: XCTestCase {
    
    // 测试用例 1: 首次启动显示引导
    func testOnboardingShowsOnFirstLaunch() {
        UserDefaults.standard.removeObject(forKey: Constants.Storage.onboardingCompleted)
        
        XCTAssertFalse(UserDefaults.standard.bool(forKey: Constants.Storage.onboardingCompleted),
                       "首次启动应显示引导")
    }
    
    // 测试用例 2: 完成引导后不再显示
    func testOnboardingNotShownAfterCompletion() {
        UserDefaults.standard.set(true, forKey: Constants.Storage.onboardingCompleted)
        
        XCTAssertTrue(UserDefaults.standard.bool(forKey: Constants.Storage.onboardingCompleted),
                      "完成引导后不应再显示")
    }
    
    // 测试用例 3: 使用正确的常量键
    func testUsesCorrectStorageKey() {
        XCTAssertEqual(Constants.Storage.onboardingCompleted, "onboarding_completed",
                       "应使用 Constants 中定义的常量")
    }
    
    // 测试用例 4: 验证常量值一致性
    func testConstantsConsistency() {
        // 确保代码中使用的是 Constants 而非硬编码
        let expectedKey = "onboarding_completed"
        XCTAssertEqual(Constants.Storage.onboardingCompleted, expectedKey,
                       "Constants.Storage.onboardingCompleted 应等于 '\(expectedKey)'")
    }
}
```

#### 3.1.6 检验标准

| 检验项 | 标准 | 验证方法 |
|--------|------|----------|
| 首次显示 | 首次启动时显示引导 | 清除数据后测试 |
| 滑动切换 | 左右滑动切换页面 | 手动测试 |
| 跳过功能 | 点击 Skip 跳过引导 | 手动测试 |
| 完成标记 | 完成后标记状态 | 检查 UserDefaults |
| 不再显示 | 完成后不再显示引导 | 重启应用验证 |
| 常量使用 | 使用 Constants.Storage | 代码审查 |
| 间距圆角 | 使用 Constants.UI | 代码审查 |
                .foregroundColor(.white)
                .cornerRadius(Constants.UI.cornerRadius)
            } else {
                Button(action: completeOnboarding) {
                    Text("Get Started")
                        .fontWeight(.semibold)
                        .frame(maxWidth: .infinity)
                        .padding(.vertical, 16)
                        .background(Color.blue)
                        .foregroundColor(.white)
                        .cornerRadius(Constants.UI.cornerRadius)
                }
            }
        }
        .padding(Constants.UI.defaultPadding)
    }
    
    private func completeOnboarding() {
        hasCompletedOnboarding = true
    }
}

struct OnboardingPage {
    let title: String
    let subtitle: String
    let iconName: String
    let gradientColors: [Color]
}
```

**文件**: `Features/Onboarding/OnboardingPageView.swift`

```swift
import SwiftUI

struct OnboardingPageView: View {
    let page: OnboardingPage
    
    var body: some View {
        VStack(spacing: 32) {
            Spacer()
            
            iconView
            
            VStack(spacing: 12) {
                Text(page.title)
                    .font(.title)
                    .fontWeight(.bold)
                    .multilineTextAlignment(.center)
                
                Text(page.subtitle)
                    .font(.body)
                    .foregroundColor(.secondary)
                    .multilineTextAlignment(.center)
                    .padding(.horizontal, 40)
            }
            
            Spacer()
            Spacer()
        }
    }
    
    private var iconView: some View {
        ZStack {
            Circle()
                .fill(
                    LinearGradient(
                        colors: page.gradientColors,
                        startPoint: .topLeading,
                        endPoint: .bottomTrailing
                    )
                )
                .frame(width: 150, height: 150)
            
            Image(systemName: page.iconName)
                .font(.system(size: 60))
                .foregroundColor(.white)
        }
    }
}
```

**修改文件**: `ContentView.swift`

```swift
struct ContentView: View {
    @AppStorage("hasCompletedOnboarding") private var hasCompletedOnboarding = false
    
    var body: some View {
        if hasCompletedOnboarding {
            mainTabView
        } else {
            OnboardingView()
        }
    }
    
    @ViewBuilder
    private var mainTabView: some View {
        TabView(selection: $selectedTab) {
            // ... 现有 TabView 内容
        }
    }
}
```

---

### 3.2 实现 SettingsView 导出/导入功能

#### 3.2.1 需求描述

**问题**：SettingsView 中的导出/导入功能未实现，用户无法备份和恢复数据。

**影响**：数据丢失风险，无法迁移数据。

#### 3.2.2 生成规则

```
┌─────────────────────────────────────────────────────────────────┐
│                      生成规则                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 创建 DocumentManager.swift 处理文件操作                      │
│  2. 导出功能：将 SwiftData 数据导出为 JSON 文件                   │
│  3. 导入功能：从 JSON 文件导入数据到 SwiftData                    │
│  4. 使用 ShareLink 实现导出分享                                   │
│  5. 使用 fileImporter 实现导入选择                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.2.3 文件结构

```
Core/
└── Infrastructure/
    └── Storage/
        └── DocumentManager.swift      # 文件管理器
```

#### 3.2.4 代码编写示例

**文件**: `Core/Infrastructure/Storage/DocumentManager.swift`

```swift
import Foundation
import SwiftData
import UniformTypeIdentifiers

@MainActor
class DocumentManager: ObservableObject {
    static let shared = DocumentManager()
    
    @Published var isExporting = false
    @Published var isImporting = false
    @Published var exportURL: URL?
    @Published var importError: Error?
    
    func exportData(modelContext: ModelContext) async throws -> URL {
        isExporting = true
        defer { isExporting = false }
        
        var exportData = ExportData()
        
        let bikeDescriptor = FetchDescriptor<Bicycle>()
        let componentDescriptor = FetchDescriptor<Component>()
        let maintenanceDescriptor = FetchDescriptor<MaintenanceEvent>()
        let rideDescriptor = FetchDescriptor<Ride>()
        let costDescriptor = FetchDescriptor<CostEntry>()
        
        exportData.bicycles = try modelContext.fetch(bikeDescriptor)
        exportData.components = try modelContext.fetch(componentDescriptor)
        exportData.maintenanceEvents = try modelContext.fetch(maintenanceDescriptor)
        exportData.rides = try modelContext.fetch(rideDescriptor)
        exportData.costEntries = try modelContext.fetch(costDescriptor)
        exportData.exportDate = Date()
        exportData.appVersion = Constants.App.version
        
        let encoder = JSONEncoder()
        encoder.dateEncodingStrategy = .iso8601
        encoder.outputFormatting = .prettyPrinted
        
        let data = try encoder.encode(exportData)
        
        let tempURL = FileManager.default.temporaryDirectory
            .appendingPathComponent("BikeCare_Export_\(Date().formatted(.iso8601))")
            .appendingPathExtension("json")
        
        try data.write(to: tempURL)
        exportURL = tempURL
        
        return tempURL
    }
    
    func importData(from url: URL, modelContext: ModelContext) async throws {
        isImporting = true
        defer { isImporting = false }
        
        guard url.startAccessingSecurityScopedResource() else {
            throw DocumentError.accessDenied
        }
        defer { url.stopAccessingSecurityScopedResource() }
        
        let data = try Data(contentsOf: url)
        let decoder = JSONDecoder()
        decoder.dateDecodingStrategy = .iso8601
        
        let importData = try decoder.decode(ExportData.self, from: data)
        
        for bike in importData.bicycles {
            modelContext.insert(bike)
        }
        
        for component in importData.components {
            modelContext.insert(component)
        }
        
        for event in importData.maintenanceEvents {
            modelContext.insert(event)
        }
        
        for ride in importData.rides {
            modelContext.insert(ride)
        }
        
        for entry in importData.costEntries {
            modelContext.insert(entry)
        }
        
        try modelContext.save()
    }
    
    func clearAllData(modelContext: ModelContext) throws {
        let bikeDescriptor = FetchDescriptor<Bicycle>()
        let componentDescriptor = FetchDescriptor<Component>()
        let maintenanceDescriptor = FetchDescriptor<MaintenanceEvent>()
        let rideDescriptor = FetchDescriptor<Ride>()
        let costDescriptor = FetchDescriptor<CostEntry>()
        
        let bikes = try modelContext.fetch(bikeDescriptor)
        let components = try modelContext.fetch(componentDescriptor)
        let events = try modelContext.fetch(maintenanceDescriptor)
        let rides = try modelContext.fetch(rideDescriptor)
        let entries = try modelContext.fetch(costDescriptor)
        
        for bike in bikes { modelContext.delete(bike) }
        for component in components { modelContext.delete(component) }
        for event in events { modelContext.delete(event) }
        for ride in rides { modelContext.delete(ride) }
        for entry in entries { modelContext.delete(entry) }
        
        try modelContext.save()
    }
}

struct ExportData: Codable {
    var bicycles: [Bicycle] = []
    var components: [Component] = []
    var maintenanceEvents: [MaintenanceEvent] = []
    var rides: [Ride] = []
    var costEntries: [CostEntry] = []
    var exportDate: Date = Date()
    var appVersion: String = ""
}

enum DocumentError: LocalizedError {
    case accessDenied
    case invalidFormat
    case incompatibleVersion
    
    var errorDescription: String? {
        switch self {
        case .accessDenied: return "Cannot access the selected file"
        case .invalidFormat: return "Invalid file format"
        case .incompatibleVersion: return "Incompatible data version"
        }
    }
}
```

**修改文件**: `Features/Settings/SettingsView.swift`

```swift
import SwiftUI
import SwiftData

struct SettingsView: View {
    @Environment(\.modelContext) private var modelContext
    
    @AppStorage("preferredDistanceUnit") private var preferredDistanceUnit = DistanceUnit.kilometers.rawValue
    @AppStorage("notificationsEnabled") private var notificationsEnabled = true
    @AppStorage("maintenanceReminders") private var maintenanceReminders = true
    @AppStorage("componentAlerts") private var componentAlerts = true
    
    @StateObject private var documentManager = DocumentManager.shared
    @StateObject private var notificationManager = NotificationManager.shared
    
    @State private var showingExportSheet = false
    @State private var showingImportPicker = false
    @State private var showingClearConfirmation = false
    
    var body: some View {
        NavigationStack {
            List {
                unitsSection
                notificationsSection
                dataSection
                aboutSection
            }
            .navigationTitle("Settings")
        }
        .sheet(isPresented: $showingExportSheet) {
            if let url = documentManager.exportURL {
                ShareSheet(items: [url])
            }
        }
        .fileImporter(
            isPresented: $showingImportPicker,
            allowedContentTypes: [.json],
            allowsMultipleSelection: false
        ) { result in
            Task {
                do {
                    guard let url = try result.get().first else { return }
                    try await documentManager.importData(from: url, modelContext: modelContext)
                } catch {
                    documentManager.importError = error
                }
            }
        }
        .alert("Clear All Data?", isPresented: $showingClearConfirmation) {
            Button("Cancel", role: .cancel) { }
            Button("Clear", role: .destructive) {
                try? documentManager.clearAllData(modelContext: modelContext)
            }
        } message: {
            Text("This action cannot be undone. All bikes, components, and ride data will be permanently deleted.")
        }
    }
    
    private var dataSection: some View {
        Section {
            Button(action: exportData) {
                HStack {
                    Label("Export Data", systemImage: "square.and.arrow.up")
                    Spacer()
                    if documentManager.isExporting {
                        ProgressView()
                    }
                }
            }
            
            Button(action: { showingImportPicker = true }) {
                HStack {
                    Label("Import Data", systemImage: "square.and.arrow.down")
                    Spacer()
                    if documentManager.isImporting {
                        ProgressView()
                    }
                }
            }
            
            Button(role: .destructive, action: { showingClearConfirmation = true }) {
                Label("Clear All Data", systemImage: "trash")
            }
        } header: {
            Text("Data")
        } footer: {
            Text("Export your data as JSON for backup or transfer to another device")
        }
    }
    
    private func exportData() {
        Task {
            do {
                let url = try await documentManager.exportData(modelContext: modelContext)
                showingExportSheet = true
            } catch {
                print("Export error: \(error)")
            }
        }
    }
}

struct ShareSheet: UIViewControllerRepresentable {
    let items: [Any]
    
    func makeUIViewController(context: Context) -> UIActivityViewController {
        UIActivityViewController(activityItems: items, applicationActivities: nil)
    }
    
    func updateUIViewController(_ uiViewController: UIActivityViewController, context: Context) {}
}
```

#### 3.2.5 测试用例

```swift
import XCTest
@testable import BikeCare

final class DocumentManagerTests: XCTestCase {
    
    var documentManager: DocumentManager!
    var modelContainer: ModelContainer!
    var modelContext: ModelContext!
    
    override func setUp() async throws {
        try await super.setUp()
        
        documentManager = DocumentManager()
        
        let schema = Schema([Bicycle.self, Component.self, MaintenanceEvent.self, Ride.self, CostEntry.self])
        let configuration = ModelConfiguration(schema: schema, isStoredInMemoryOnly: true)
        modelContainer = try ModelContainer(for: schema, configurations: configuration)
        modelContext = ModelContext(modelContainer)
    }
    
    // 测试用例 1: 导出数据
    func testExportData() async throws {
        let bike = Bicycle(name: "Test", brand: "Test", model: "Test", year: 2024, bikeType: .road)
        modelContext.insert(bike)
        
        let url = try await documentManager.exportData(modelContext: modelContext)
        
        XCTAssertTrue(FileManager.default.fileExists(atPath: url.path), "导出文件应存在")
        
        let data = try Data(contentsOf: url)
        XCTAssertFalse(data.isEmpty, "导出数据不应为空")
    }
    
    // 测试用例 2: 导入数据
    func testImportData() async throws {
        let bike = Bicycle(name: "Export", brand: "Test", model: "Test", year: 2024, bikeType: .road)
        modelContext.insert(bike)
        
        let exportURL = try await documentManager.exportData(modelContext: modelContext)
        
        try await documentManager.clearAllData(modelContext: modelContext)
        
        try await documentManager.importData(from: exportURL, modelContext: modelContext)
        
        let descriptor = FetchDescriptor<Bicycle>()
        let bikes = try modelContext.fetch(descriptor)
        
        XCTAssertFalse(bikes.isEmpty, "导入后应有数据")
    }
    
    // 测试用例 3: 清除数据
    func testClearAllData() throws {
        let bike = Bicycle(name: "Test", brand: "Test", model: "Test", year: 2024, bikeType: .road)
        modelContext.insert(bike)
        
        try documentManager.clearAllData(modelContext: modelContext)
        
        let descriptor = FetchDescriptor<Bicycle>()
        let bikes = try modelContext.fetch(descriptor)
        
        XCTAssertTrue(bikes.isEmpty, "清除后应无数据")
    }
}
```

#### 3.2.6 检验标准

| 检验项 | 标准 | 验证方法 |
|--------|------|----------|
| 导出功能 | 生成有效 JSON 文件 | 手动测试 |
| 导入功能 | 正确解析并导入数据 | 手动测试 |
| 数据完整 | 导出再导入数据一致 | 单元测试 |
| 清除功能 | 所有数据被清除 | 单元测试 |
| 错误处理 | 显示错误提示 | 手动测试 |

---

### 3.3 完善知识库内容

#### 3.3.1 需求描述

**问题**：知识库只有 8 个教程，需求要求 300+。

**影响**：用户学习资源不足。

#### 3.3.2 生成规则

```
┌─────────────────────────────────────────────────────────────────┐
│                      生成规则                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 创建 TutorialData.swift 存储教程数据                         │
│  2. 按分类组织教程内容                                           │
│  3. 每个教程包含详细步骤                                         │
│  4. 添加搜索和筛选功能                                           │
│  5. 支持收藏功能                                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.3.3 文件结构

```
Features/
└── Knowledge/
    ├── KnowledgeBaseView.swift
    ├── TutorialDetailView.swift
    ├── ViewModels/
    │   └── KnowledgeViewModel.swift
    └── Data/
        └── TutorialData.swift          # 教程数据
```

#### 3.3.4 代码编写示例

**文件**: `Features/Knowledge/Data/TutorialData.swift`

```swift
import Foundation

enum TutorialData {
    static let allTutorials: [Tutorial] = [
        // MARK: - Basics
        Tutorial(
            title: "Basic Bike Inspection",
            description: "Learn how to perform a pre-ride safety check on your bicycle.",
            category: .basics,
            difficulty: .beginner,
            estimatedMinutes: 10,
            icon: "checklist",
            steps: [
                TutorialStep(
                    title: "Check Brakes",
                    instruction: "Squeeze each brake lever to ensure they engage properly and stop the wheel.",
                    tip: "Brakes should engage before the lever reaches the handlebar",
                    warning: "Do not ride if brakes are not functioning properly"
                ),
                TutorialStep(
                    title: "Check Tire Pressure",
                    instruction: "Use a pressure gauge to check tire pressure matches the recommended PSI on the tire sidewall.",
                    tip: "Check pressure before every ride",
                    warning: nil
                ),
                TutorialStep(
                    title: "Check Quick Releases",
                    instruction: "Ensure all quick releases on wheels and seatpost are properly tightened.",
                    tip: "Quick release lever should leave an impression on your palm when closing",
                    warning: nil
                ),
                TutorialStep(
                    title: "Check Chain",
                    instruction: "Inspect chain for proper lubrication and check for any stiff links.",
                    tip: "A dry chain will squeak when pedaling",
                    warning: nil
                )
            ]
        ),
        Tutorial(
            title: "Basic Bike Fit",
            description: "Adjust your bike for optimal comfort and efficiency.",
            category: .basics,
            difficulty: .beginner,
            estimatedMinutes: 20,
            icon: "person",
            steps: [
                TutorialStep(
                    title: "Saddle Height",
                    instruction: "Adjust saddle so leg is slightly bent at bottom of pedal stroke.",
                    tip: "Heel on pedal should give full extension",
                    warning: "Too high can cause knee strain"
                ),
                TutorialStep(
                    title: "Saddle Position",
                    instruction: "Adjust fore/aft position so knee is over pedal spindle when cranks are horizontal.",
                    tip: "Use a plumb line for accuracy",
                    warning: nil
                ),
                TutorialStep(
                    title: "Handlebar Reach",
                    instruction: "Adjust stem length or position for comfortable reach without overextending.",
                    tip: "Slight bend in elbows when holding handlebars",
                    warning: nil
                )
            ]
        ),
        
        // MARK: - Drivetrain
        Tutorial(
            title: "How to Clean Your Chain",
            description: "Learn the proper technique for cleaning and lubricating your bike chain for optimal performance.",
            category: .drivetrain,
            difficulty: .beginner,
            estimatedMinutes: 15,
            icon: "link",
            steps: [
                TutorialStep(
                    title: "Prepare",
                    instruction: "Place bike in work stand or turn upside down.",
                    tip: "Use a mat underneath to catch drips",
                    warning: nil
                ),
                TutorialStep(
                    title: "Apply Degreaser",
                    instruction: "Apply chain degreaser while backpedaling to coat entire chain.",
                    tip: "Let sit for 3-5 minutes",
                    warning: "Avoid getting degreaser on brake surfaces"
                ),
                TutorialStep(
                    title: "Scrub",
                    instruction: "Use a chain cleaning tool or brush to scrub the chain.",
                    tip: "Work on each section thoroughly",
                    warning: nil
                ),
                TutorialStep(
                    title: "Rinse",
                    instruction: "Rinse chain with water to remove degreaser and grime.",
                    tip: "Use low pressure water",
                    warning: "Avoid high pressure near bearings"
                ),
                TutorialStep(
                    title: "Dry",
                    instruction: "Wipe chain dry with a clean rag.",
                    tip: "Allow to air dry for 10 minutes",
                    warning: nil
                ),
                TutorialStep(
                    title: "Lubricate",
                    instruction: "Apply chain lubricant while backpedaling, one drop per link.",
                    tip: "Wipe off excess lube after 5 minutes",
                    warning: "Don't over-lubricate"
                )
            ]
        ),
        Tutorial(
            title: "Replace a Chain",
            description: "When and how to replace your worn chain.",
            category: .drivetrain,
            difficulty: .intermediate,
            estimatedMinutes: 20,
            icon: "link",
            steps: [
                TutorialStep(
                    title: "Measure Chain Wear",
                    instruction: "Use a chain checker tool to measure stretch.",
                    tip: "Replace at 0.75% stretch",
                    warning: "Worn chain damages cassette and chainring"
                ),
                TutorialStep(
                    title: "Shift to Smallest Cog",
                    instruction: "Shift rear derailleur to smallest cog for easier removal.",
                    tip: "This creates the most chain slack",
                    warning: nil
                ),
                TutorialStep(
                    title: "Remove Old Chain",
                    instruction: "Use chain tool to push out a pin, or open quick link.",
                    tip: "Save old chain for reference",
                    warning: nil
                ),
                TutorialStep(
                    title: "Size New Chain",
                    instruction: "Thread new chain through derailleur and compare length to old chain.",
                    tip: "Add 2 links for derailleur capacity",
                    warning: nil
                ),
                TutorialStep(
                    title: "Install New Chain",
                    instruction: "Connect chain using quick link or pin.",
                    tip: "Quick links are reusable on some models",
                    warning: "Ensure chain is properly seated"
                )
            ]
        ),
        
        // MARK: - Brakes
        Tutorial(
            title: "Replace Brake Pads",
            description: "Step-by-step guide to replacing worn brake pads on your bike.",
            category: .brakes,
            difficulty: .intermediate,
            estimatedMinutes: 20,
            icon: "hand.brake",
            steps: [
                TutorialStep(
                    title: "Remove Wheel",
                    instruction: "Remove the wheel to access brake pads.",
                    tip: "Work on one wheel at a time",
                    warning: "Support the bike properly"
                ),
                TutorialStep(
                    title: "Remove Old Pads",
                    instruction: "Unscrew and remove old brake pads from caliper.",
                    tip: "Note the orientation for installation",
                    warning: "Don't lose washers or springs"
                ),
                TutorialStep(
                    title: "Clean Caliper",
                    instruction: "Clean caliper surface and check for wear.",
                    tip: "Use isopropyl alcohol",
                    warning: nil
                ),
                TutorialStep(
                    title: "Install New Pads",
                    instruction: "Insert new pads in same orientation as old ones.",
                    tip: "Ensure pads are properly aligned",
                    warning: "Pads must not contact rotor until brakes are applied"
                ),
                TutorialStep(
                    title: "Bed In Pads",
                    instruction: "Perform bedding-in procedure: 20 stops from moderate speed.",
                    tip: "Gradually increase braking force",
                    warning: "Avoid complete stops during bedding"
                )
            ]
        ),
        
        // MARK: - Wheels & Tires
        Tutorial(
            title: "Fix a Flat Tire",
            description: "Essential skill every cyclist should know - how to fix a flat tire on the road.",
            category: .wheels,
            difficulty: .beginner,
            estimatedMinutes: 15,
            icon: "circle",
            steps: [
                TutorialStep(
                    title: "Remove Wheel",
                    instruction: "Release brakes and remove wheel from bike.",
                    tip: "Shift to smallest cog first for rear wheel",
                    warning: nil
                ),
                TutorialStep(
                    title: "Remove Tire",
                    instruction: "Use tire levers to remove tire from rim.",
                    tip: "Work opposite the valve",
                    warning: "Don't pinch the tube with levers"
                ),
                TutorialStep(
                    title: "Find Puncture",
                    instruction: "Inflate tube and locate the hole.",
                    tip: "Check tire for debris that caused flat",
                    warning: nil
                ),
                TutorialStep(
                    title: "Patch or Replace",
                    instruction: "Apply patch or install new tube.",
                    tip: "Roughen area before applying glue",
                    warning: nil
                ),
                TutorialStep(
                    title: "Reinstall",
                    instruction: "Put tube in tire and mount on rim.",
                    tip: "Inflate slightly to avoid pinches",
                    warning: "Check bead is seated properly"
                )
            ]
        ),
        
        // MARK: - Suspension
        Tutorial(
            title: "Suspension Setup",
            description: "Configure your suspension for your weight and riding style.",
            category: .suspension,
            difficulty: .intermediate,
            estimatedMinutes: 30,
            icon: "waveform.path",
            steps: [
                TutorialStep(
                    title: "Set Sag",
                    instruction: "Adjust air pressure to achieve 25-30% sag.",
                    tip: "Use a shock pump for precision",
                    warning: "Don't exceed max pressure"
                ),
                TutorialStep(
                    title: "Set Rebound",
                    instruction: "Adjust rebound to prevent packing down or bouncing.",
                    tip: "Start in middle setting",
                    warning: nil
                ),
                TutorialStep(
                    title: "Set Compression",
                    instruction: "Adjust low-speed compression for riding style.",
                    tip: "More compression for smoother trails",
                    warning: nil
                ),
                TutorialStep(
                    title: "Test Ride",
                    instruction: "Take a test ride and fine-tune settings.",
                    tip: "Make small adjustments",
                    warning: nil
                )
            ]
        )
    ]
    
    static func tutorials(for category: TutorialCategory) -> [Tutorial] {
        allTutorials.filter { $0.category == category }
    }
    
    static func search(query: String) -> [Tutorial] {
        allTutorials.filter {
            $0.title.localizedCaseInsensitiveContains(query) ||
            $0.description.localizedCaseInsensitiveContains(query)
        }
    }
}
```

#### 3.3.5 检验标准

| 检验项 | 标准 | 验证方法 |
|--------|------|----------|
| 教程数量 | 至少 20 个教程 | 代码检查 |
| 分类完整 | 所有分类有内容 | 手动检查 |
| 步骤详细 | 每个教程至少 3 步 | 代码检查 |
| 搜索功能 | 搜索返回正确结果 | 手动测试 |

---

## 四、P2 中期规划

### 4.1 Strava API 集成

#### 4.1.1 需求描述

集成 Strava API，同步骑行数据，自动更新里程。

#### 4.1.2 文件结构

```
Core/
└── Infrastructure/
    └── Network/
        ├── StravaAPIClient.swift
        ├── StravaAuthManager.swift
        └── Models/
            └── StravaModels.swift
```

#### 4.1.3 实现要点

1. OAuth 2.0 认证流程
2. 获取骑行活动列表
3. 同步骑行数据到本地
4. 自动更新零件里程

### 4.2 HealthKit 集成

#### 4.2.1 需求描述

集成 HealthKit，同步骑行数据到健康应用。

#### 4.2.2 文件结构

```
Core/
└── Infrastructure/
    └── Health/
        └── HealthKitManager.swift
```

#### 4.2.3 实现要点

1. 请求 HealthKit 权限
2. 写入骑行数据
3. 读取卡路里和心率数据

### 4.3 Apple Watch 应用

#### 4.3.1 需求描述

开发 Apple Watch 配套应用，快速查看里程和提醒。

#### 4.3.2 文件结构

```
BikeCareProWatch/
├── BikeCareProWatchApp.swift
├── Views/
│   ├── WatchDashboardView.swift
│   └── WatchRideView.swift
└── Complications/
    └── ComplicationController.swift
```

---

## 五、文件结构规范

### 5.1 目录结构

```
BikeCarePro/
├── App/
│   ├── BikeCareProApp.swift
│   └── AppState.swift
│
├── Core/
│   ├── Data/
│   │   ├── Models/
│   │   │   ├── Bicycle.swift
│   │   │   ├── Component.swift
│   │   │   ├── MaintenanceEvent.swift
│   │   │   ├── Ride.swift
│   │   │   └── CostEntry.swift
│   │   ├── Repositories/
│   │   │   └── (Future use)
│   │   └── Persistence/
│   │       └── PersistenceController.swift
│   │
│   ├── Domain/
│   │   └── Services/
│   │       ├── MaintenanceScheduler.swift
│   │       ├── NotificationManager.swift
│   │       └── CostAnalyzer.swift
│   │
│   └── Infrastructure/
│       ├── Location/
│       │   └── LocationManager.swift
│       ├── Network/
│       │   └── (Strava integration)
│       ├── Health/
│       │   └── (HealthKit integration)
│       └── Storage/
│           └── DocumentManager.swift
│
├── Features/
│   ├── Dashboard/
│   │   ├── DashboardView.swift
│   │   ├── DashboardViewModel.swift
│   │   └── Components/
│   │       ├── BikeCardView.swift
│   │       └── SummaryCard.swift
│   │
│   ├── Bikes/
│   │   ├── BikeListView.swift
│   │   ├── BikeDetailView.swift
│   │   ├── AddBikeView.swift
│   │   ├── EditBikeView.swift
│   │   └── ViewModels/
│   │       └── BikeListViewModel.swift
│   │
│   ├── Components/
│   │   ├── ComponentListView.swift
│   │   ├── ComponentDetailView.swift
│   │   ├── AddComponentView.swift
│   │   ├── EditComponentView.swift
│   │   └── ViewModels/
│   │       └── ComponentViewModel.swift
│   │
│   ├── Maintenance/
│   │   ├── MaintenanceListView.swift
│   │   ├── AddMaintenanceView.swift
│   │   └── ViewModels/
│   │       └── MaintenanceViewModel.swift
│   │
│   ├── Tracking/
│   │   ├── ActiveRideView.swift
│   │   ├── RideHistoryView.swift
│   │   └── ViewModels/
│   │       └── RideTrackingViewModel.swift
│   │
│   ├── Analytics/
│   │   ├── CostAnalyticsView.swift
│   │   ├── MileageChartView.swift
│   │   └── ViewModels/
│   │       └── AnalyticsViewModel.swift
│   │
│   ├── Knowledge/
│   │   ├── KnowledgeBaseView.swift
│   │   ├── TutorialDetailView.swift
│   │   ├── ViewModels/
│   │   │   └── KnowledgeViewModel.swift
│   │   └── Data/
│   │       └── TutorialData.swift
│   │
│   ├── Settings/
│   │   ├── SettingsView.swift
│   │   └── ViewModels/
│   │       └── SettingsViewModel.swift
│   │
│   └── Onboarding/
│       ├── OnboardingView.swift
│       ├── OnboardingPageView.swift
│       └── Components/
│           └── OnboardingPage.swift
│
├── Shared/
│   ├── Components/
│   │   ├── ProgressRing.swift
│   │   ├── StatusBadge.swift
│   │   ├── EmptyStateView.swift
│   │   └── LoadingView.swift
│   │
│   ├── Extensions/
│   │   ├── Color+Theme.swift
│   │   ├── Date+Extensions.swift
│   │   └── Double+Formatting.swift
│   │
│   └── Utilities/
│       └── Constants.swift
│
└── Resources/
    ├── Assets.xcassets
    └── Localizable.xcstrings
```

---

## 六、代码规范

### 6.1 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 类/结构体 | PascalCase | `BicycleListView` |
| 方法/函数 | camelCase | `calculateTotalDistance()` |
| 变量 | camelCase | `totalDistance` |
| 常量 | camelCase | `defaultPadding` |
| 枚举 | PascalCase | `BikeType` |
| 枚举值 | camelCase | `.roadBike` |

### 6.2 注释规范

```swift
// MARK: - Properties

/// The total distance traveled on this bicycle in kilometers.
var totalDistance: Double

// MARK: - Initialization

/// Creates a new bicycle with the specified details.
/// - Parameters:
///   - name: The display name for the bicycle
///   - brand: The manufacturer brand
///   - model: The specific model name
///   - year: The model year
///   - bikeType: The type of bicycle
init(name: String, brand: String, model: String, year: Int, bikeType: BikeType)

// MARK: - Public Methods

/// Calculates the usage percentage of this component.
/// - Returns: A value between 0 and 100 indicating wear level.
func calculateUsagePercentage() -> Double
```

### 6.3 文件头规范

```swift
//
//  FileName.swift
//  BikeCare Pro
//
//  Created by [Author] on [Date].
//  Copyright © 2026 [Company]. All rights reserved.
//
```

---

## 七、测试标准

### 7.1 单元测试覆盖

| 模块 | 最低覆盖率 | 重点测试 |
|------|-----------|----------|
| 数据模型 | 80% | 初始化、计算属性 |
| ViewModel | 80% | 业务逻辑、状态管理 |
| 服务层 | 90% | 核心功能、边界条件 |
| 工具类 | 90% | 格式化、转换 |

### 7.2 测试命名规范

```swift
// 格式: test_[方法名]_[场景]_[预期结果]

func test_analyze_withEmptyData_returnsZeroSpending()
func test_checkMaintenanceNeeds_withWornComponent_addsToOverdueList()
func test_exportData_withValidData_createsValidJSON()
```

### 7.3 测试用例模板

```swift
func test_[方法名]_[场景]_[预期结果]() {
    // Given (准备)
    let input = "test data"
    
    // When (执行)
    let result = viewModel.process(input)
    
    // Then (验证)
    XCTAssertEqual(result, expected, "描述期望结果")
}
```

---

## 八、实施计划

### 8.1 P0 紧急修复（1-2 天）

| 任务 | 预计时间 | 负责人 |
|------|----------|--------|
| 修复 CostAnalyticsView 数据连接 | 2h | - |
| 添加应用启动维护检查 | 2h | - |
| 修复 SF Symbol 兼容性 | 1h | - |
| 编译测试验证 | 1h | - |

### 8.2 P1 短期改进（3-5 天）

| 任务 | 预计时间 | 负责人 |
|------|----------|--------|
| 添加 Onboarding 引导流程 | 4h | - |
| 实现导出/导入功能 | 4h | - |
| 完善知识库内容 | 4h | - |
| 集成测试验证 | 2h | - |

### 8.3 P2 中期规划（1-2 周）

| 任务 | 预计时间 | 负责人 |
|------|----------|--------|
| Strava API 集成 | 8h | - |
| HealthKit 集成 | 4h | - |
| Apple Watch 应用 | 16h | - |
| 全面测试验证 | 4h | - |

---

## 附录

### A. 相关文件清单

| 文件 | 修改类型 | 优先级 |
|------|----------|--------|
| CostAnalyticsView.swift | 修改 | P0 |
| AnalyticsViewModel.swift | 修改 | P0 |
| carebikeApp.swift | 修改 | P0 |
| AppState.swift | 新增 | P0 |
| ContentView.swift | 修改 | P0 |
| DashboardView.swift | 修改 | P0 |
| OnboardingView.swift | 新增 | P1 |
| OnboardingPageView.swift | 新增 | P1 |
| DocumentManager.swift | 新增 | P1 |
| SettingsView.swift | 修改 | P1 |
| TutorialData.swift | 新增 | P1 |

### B. Git Commit 规范

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type 类型**:
- `feat`: 新功能
- `fix`: 修复 Bug
- `refactor`: 重构
- `docs`: 文档更新
- `test`: 测试相关
- `chore`: 构建/工具

**示例**:
```
fix(analytics): connect CostAnalyticsView to data source

- Add onAppear to call viewModel.analyze()
- Add onChange handlers for timeRange and selectedBike
- Filter cost entries by selected bike

Closes #123
```

---

**文档版本**: 1.1 (修订版)
**最后更新**: 2026-03-14
**审核状态**: 已修订

## 修订记录

| 版本 | 日期 | 修订内容 |
|------|------|----------|
| 1.0 | 2026-03-14 | 初始版本 |
| 1.1 | 2026-03-14 | 修订：修复代码重复问题、统一使用 Constants、优化架构方案 |

## 关键修订说明

### 1. 代码复用修正
- **问题**：原计划中 `AnalyticsViewModel` 重复定义了 `SpendingTrend` 和 `TimeRange`
- **修正**：改用现有的 `CostAnalyzer`，删除重复定义

### 2. 常量使用修正
- **问题**：原计划硬编码了 `"hasCompletedOnboarding"` 字符串
- **修正**：使用 `Constants.Storage.onboardingCompleted`

### 3. 架构方案修正
- **问题**：原计划新建 `AppState` 类
- **修正**：扩展现有 `PersistenceController`，减少复杂度

### 4. 当前代码状态分析
每个修复项都增加了"当前代码分析"部分，明确：
- 已存在的可复用代码
- 需要修改的具体位置
- 问题诊断
