# BikeCare Pro 改进计划 2.0

> **版本**: 2.0  
> **修订日期**: 2026-03-14  
> **目标**: 基于流程分析结果，优化软件架构和用户体验  
> **原则**: 单一职责、高内聚低耦合、代码复用、最小代码、风格一致、优先继承

---

## 修订记录

| 日期 | 版本 | 修订内容 |
|------|------|----------|
| 2026-03-14 | 2.0 | 基于现有代码审查，修复架构兼容性问题 |

---

## 目录

1. [改进原则](#一改进原则)
2. [问题分析](#二问题分析)
3. [P1 架构优化](#三p1-架构优化)
4. [P2 功能完善](#四p2-功能完善)
5. [P3 体验提升](#五p3-体验提升)
6. [文件结构规范](#六文件结构规范)
7. [代码规范](#七代码规范)
8. [测试标准](#八测试标准)
9. [实施计划](#九实施计划)

---

## 一、改进原则

### 1.1 核心原则

| 原则 | 说明 | 实践方式 |
|------|------|----------|
| **单一职责** | 一个功能一个模块 | 每个文件只做一件事，函数不超过50行 |
| **高内聚低耦合** | 相关功能集中，模块间依赖最小 | 使用协议/接口解耦，避免直接依赖具体实现 |
| **代码复用** | 优先复用现有模块 | 符合"三次法则"，遇到三次重复再抽象 |
| **最小代码** | 删除废弃/冗余代码 | 替换时标记废弃 → 验证无影响 → 删除 |
| **风格一致** | 与现有软件风格统一 | 使用相同的命名规范、UI组件、常量 |
| **优先继承** | 利用现有继承关系 | Settings等功能优先继承现有实现 |

### 1.2 需求描述规范

每个优化项必须包含：
- **需求描述**: 解决什么问题
- **实现方案**: 如何修改
- **代码示例**: 关键代码片段
- **测试用例**: 验证方式
- **检验标准**: 完成标准

---

## 二、问题分析

### 2.1 现有代码审查发现的问题

| 问题 | 位置 | 影响 | 修复方案 |
|------|------|------|----------|
| **StartRideView 内联定义** | ContentView.swift | 违反单一职责 | 提取为独立文件 |
| **RideTrackingViewModel 未使用** | ActiveRideView.swift | 直接使用 LocationManager，业务逻辑分散 | 保持现状，RideTrackingViewModel 用于其他场景 |
| **缺少骑行详情页** | RideHistoryView.swift | 无法查看单次骑行详情 | 新增 RideDetailView |
| **NoRidesView 调用错误** | RideHistoryView.swift | 使用了不存在的 onStartRide 参数 | 修正参数名 |

### 2.2 架构兼容性分析

**现有架构特点**:
- `LocationManager` 是核心定位服务，已完整实现骑行追踪
- `ActiveRideView` 直接使用 `LocationManager` 是合理设计
- `RideTrackingViewModel` 可作为其他场景的辅助 ViewModel

**设计决策**:
- ✅ 保持 `ActiveRideView` 直接使用 `LocationManager` 的设计
- ✅ `RideTrackingViewModel` 保留用于数据查询和统计
- ❌ 不强制改为通过 ViewModel 中转，避免过度设计

---

## 三、P1 架构优化

### P1.1: 重构 StartRideView 独立模块

**需求描述**: 
当前 StartRideView 内联定义在 ContentView.swift 中，违反单一职责原则，不利于维护和测试。

**实现方案**:
1. 创建独立文件 `/Features/Tracking/StartRideView.swift`
2. 将 StartRideView 从 ContentView 中移出
3. 保持现有功能和UI不变
4. 添加自行车选择器功能

**代码示例**:
```swift
// StartRideView.swift
import SwiftUI
import SwiftData

struct StartRideView: View {
    @Environment(\.modelContext) private var modelContext
    @Query(sort: \Bicycle.name) private var bicycles: [Bicycle]
    
    @State private var selectedBike: Bicycle?
    @State private var showActiveRide = false
    @State private var showingAddBike = false
    
    var body: some View {
        NavigationStack {
            Group {
                if bicycles.isEmpty {
                    emptyStateView
                } else {
                    contentView
                }
            }
            .navigationTitle("Track")
        }
        .sheet(isPresented: $showActiveRide) {
            NavigationStack {
                ActiveRideView(initialBike: selectedBike)
            }
        }
        .sheet(isPresented: $showingAddBike) {
            AddBikeView()
        }
    }
    
    private var emptyStateView: some View {
        VStack(spacing: 24) {
            Image(systemName: "bicycle")
                .font(.system(size: 80))
                .foregroundColor(.secondary)
            
            Text("No Bikes Yet")
                .font(.title)
                .fontWeight(.bold)
            
            Text("Add your first bike to start tracking rides.")
                .font(.body)
                .foregroundColor(.secondary)
                .multilineTextAlignment(.center)
                .padding(.horizontal, 40)
            
            Button(action: { showingAddBike = true }) {
                Label("Add Bike", systemImage: "plus")
                    .font(.headline)
                    .foregroundColor(.white)
                    .frame(maxWidth: .infinity)
                    .padding()
                    .background(Color.blue)
                    .cornerRadius(Constants.UI.cornerRadius)
            }
            .padding(.horizontal, 40)
        }
        .padding()
    }
    
    private var contentView: some View {
        VStack(spacing: 24) {
            Image(systemName: "bicycle.circle.fill")
                .font(.system(size: 80))
                .foregroundColor(.blue)
            
            Text("Ready to Ride?")
                .font(.title)
                .fontWeight(.bold)
            
            // 自行车选择器
            VStack(alignment: .leading, spacing: 8) {
                Text("Select Bike")
                    .font(.subheadline)
                    .foregroundColor(.secondary)
                
                Picker("Bike", selection: $selectedBike) {
                    ForEach(bicycles) { bike in
                        Text(bike.name).tag(bike as Bicycle?)
                    }
                }
                .pickerStyle(.menu)
                .onAppear {
                    if selectedBike == nil {
                        selectedBike = bicycles.first
                    }
                }
            }
            .padding(.horizontal)
            
            Button(action: { showActiveRide = true }) {
                HStack {
                    Image(systemName: "play.fill")
                    Text("Start Ride")
                }
                .font(.headline)
                .foregroundColor(.white)
                .frame(maxWidth: .infinity)
                .padding()
                .background(Color.blue)
                .cornerRadius(Constants.UI.cornerRadius)
            }
            .padding(.horizontal, 40)
            
            NavigationLink(destination: RideHistoryView()) {
                Text("View Ride History")
                    .font(.subheadline)
                    .foregroundColor(.blue)
            }
        }
        .padding()
    }
}

// ContentView.swift 修改
struct ContentView: View {
    @EnvironmentObject var persistenceController: PersistenceController
    @State private var selectedTab: Tab = .dashboard
    
    enum Tab: String, CaseIterable {
        case dashboard = "Dashboard"
        case bikes = "Bikes"
        case tracking = "Track"
        case maintenance = "Maintenance"
        case more = "More"
        
        var icon: String {
            switch self {
            case .dashboard: return "house.fill"
            case .bikes: return "bicycle"
            case .tracking: return "bicycle.circle.fill"
            case .maintenance: return "wrench.and.screwdriver.fill"
            case .more: return "ellipsis.circle.fill"
            }
        }
    }
    
    var body: some View {
        TabView(selection: $selectedTab) {
            DashboardView()
                .tabItem {
                    Label(Tab.dashboard.rawValue, systemImage: Tab.dashboard.icon)
                }
                .tag(Tab.dashboard)
            
            BikeListView()
                .tabItem {
                    Label(Tab.bikes.rawValue, systemImage: Tab.bikes.icon)
                }
                .tag(Tab.bikes)
            
            StartRideView()  // 引用独立模块
                .tabItem {
                    Label(Tab.tracking.rawValue, systemImage: Tab.tracking.icon)
                }
                .tag(Tab.tracking)
            
            MaintenanceListView()
                .tabItem {
                    Label(Tab.maintenance.rawValue, systemImage: Tab.maintenance.icon)
                }
                .tag(Tab.maintenance)
            
            MoreView()
                .tabItem {
                    Label(Tab.more.rawValue, systemImage: Tab.more.icon)
                }
                .tag(Tab.more)
        }
        .tint(.blue)
        .onAppear {
            Task {
                await persistenceController.checkMaintenanceNeeds()
            }
        }
    }
}
```

**ActiveRideView 修改**:
```swift
// ActiveRideView.swift 添加初始化参数
struct ActiveRideView: View {
    let initialBike: Bicycle?  // 新增：初始选择的自行车
    
    @Environment(\.modelContext) private var modelContext
    @Environment(\.dismiss) private var dismiss
    
    @StateObject private var locationManager = LocationManager()
    @State private var isPaused = false
    @State private var selectedBicycle: Bicycle?
    
    var body: some View {
        // ... 现有代码
        .onAppear {
            selectedBicycle = initialBike ?? bicycles.first  // 使用传入的自行车
            locationManager.requestAuthorization()
            locationManager.startRide()
        }
    }
}
```

**测试用例**:
1. 编译成功，无错误
2. Track tab 正常显示
3. 无自行车时显示空状态，点击可添加
4. 有自行车时显示选择器
5. 点击 "Start Ride" 正常跳转到 ActiveRideView
6. 选择的自行车正确传递

**检验标准**:
- [ ] StartRideView.swift 文件创建成功
- [ ] ContentView.swift 中内联代码已删除
- [ ] 功能与之前完全一致
- [ ] 自行车选择器正常工作
- [ ] 编译通过

---

### P1.2: 添加 RideDetailView 骑行详情页

**需求描述**:
当前 RideHistoryView 只能查看列表，无法查看单次骑行的详细信息（路线、速度变化等）。

**实现方案**:
1. 创建 RideDetailView.swift
2. 在 RideHistoryView 中添加点击进入详情
3. 显示骑行路线、统计数据

**代码示例**:
```swift
// RideDetailView.swift
import SwiftUI
import MapKit

struct RideDetailView: View {
    let ride: Ride
    
    @State private var mapRegion: MKCoordinateRegion
    
    init(ride: Ride) {
        self.ride = ride
        
        // 初始化地图区域
        if let routePoints = ride.routePoints, let firstPoint = routePoints.first {
            _mapRegion = State(initialValue: MKCoordinateRegion(
                center: firstPoint.coordinate,
                span: MKCoordinateSpan(latitudeDelta: 0.01, longitudeDelta: 0.01)
            ))
        } else {
            _mapRegion = State(initialValue: MKCoordinateRegion(
                center: CLLocationCoordinate2D(latitude: 37.7749, longitude: -122.4194),
                span: MKCoordinateSpan(latitudeDelta: 0.1, longitudeDelta: 0.1)
            ))
        }
    }
    
    var body: some View {
        ScrollView {
            VStack(spacing: 20) {
                mapSection
                statsSection
                bikeSection
            }
            .padding()
        }
        .navigationTitle("Ride Details")
        .navigationBarTitleDisplayMode(.inline)
    }
    
    private var mapSection: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("Route")
                .font(.headline)
            
            Map(position: .constant(.region(mapRegion))) {
                if let points = ride.routePoints, !points.isEmpty {
                    MapPolyline(coordinates: points.map { $0.coordinate })
                        .stroke(.blue, lineWidth: 4)
                    
                    if let start = points.first {
                        Annotation("Start", coordinate: start.coordinate) {
                            Image(systemName: "play.circle.fill")
                                .foregroundColor(.green)
                                .font(.title)
                        }
                    }
                    
                    if let end = points.last, points.count > 1 {
                        Annotation("End", coordinate: end.coordinate) {
                            Image(systemName: "stop.circle.fill")
                                .foregroundColor(.red)
                                .font(.title)
                        }
                    }
                }
            }
            .frame(height: 250)
            .cornerRadius(Constants.UI.cornerRadius)
        }
    }
    
    private var statsSection: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("Statistics")
                .font(.headline)
            
            LazyVGrid(columns: [GridItem(.flexible()), GridItem(.flexible())], spacing: 16) {
                StatCard(
                    title: "Distance",
                    value: ride.distance.formattedDistance,
                    icon: "bicycle.circle.fill",
                    color: .blue
                )
                
                StatCard(
                    title: "Duration",
                    value: ride.formattedDuration,
                    icon: "clock",
                    color: .green
                )
                
                StatCard(
                    title: "Avg Speed",
                    value: averageSpeed.formattedSpeed,
                    icon: "speedometer",
                    color: .orange
                )
                
                StatCard(
                    title: "Max Speed",
                    value: ride.maxSpeed.formattedSpeed,
                    icon: "bolt",
                    color: .red
                )
            }
        }
    }
    
    private var bikeSection: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("Bike")
                .font(.headline)
            
            if let bike = ride.bicycle {
                HStack {
                    Image(systemName: bike.bikeType.icon)
                        .font(.title2)
                        .foregroundColor(.blue)
                    
                    VStack(alignment: .leading) {
                        Text(bike.name)
                            .font(.subheadline)
                            .fontWeight(.medium)
                        Text("\(bike.brand) \(bike.model)")
                            .font(.caption)
                            .foregroundColor(.secondary)
                    }
                    
                    Spacer()
                }
                .padding()
                .background(Color(.systemGray6))
                .cornerRadius(Constants.UI.cornerRadius)
            } else {
                Text("No bike associated")
                    .foregroundColor(.secondary)
                    .frame(maxWidth: .infinity, alignment: .center)
                    .padding()
                    .background(Color(.systemGray6))
                    .cornerRadius(Constants.UI.cornerRadius)
            }
        }
    }
    
    private var averageSpeed: Double {
        guard ride.duration > 0 else { return 0 }
        return ride.distance / (ride.duration / 3600)
    }
}

struct StatCard: View {
    let title: String
    let value: String
    let icon: String
    let color: Color
    
    var body: some View {
        VStack(spacing: 8) {
            Image(systemName: icon)
                .font(.title2)
                .foregroundColor(color)
            
            Text(value)
                .font(.title3)
                .fontWeight(.bold)
            
            Text(title)
                .font(.caption)
                .foregroundColor(.secondary)
        }
        .frame(maxWidth: .infinity)
        .padding()
        .background(Color(.systemGray6))
        .cornerRadius(Constants.UI.cornerRadius)
    }
}

// RideHistoryView.swift 修改
struct RideHistoryView: View {
    @Environment(\.modelContext) private var modelContext
    @Query(sort: \Ride.startTime, order: .reverse) private var rides: [Ride]
    
    @State private var selectedRide: Ride?  // 新增：选中的骑行
    @State private var timeRange: TimeRange = .allTime
    
    var body: some View {
        NavigationStack {
            Group {
                if rides.isEmpty {
                    // 修正：移除不存在的 onStartRide 参数
                    NoRidesView()
                } else {
                    rideList
                }
            }
            .navigationTitle("Ride History")
        }
        .sheet(item: $selectedRide) { ride in  // 新增：详情页 sheet
            NavigationStack {
                RideDetailView(ride: ride)
            }
        }
    }
    
    private var rideList: some View {
        List {
            // ... 统计 Section
            
            ForEach(groupedRides.keys.sorted().reversed(), id: \.self) { date in
                Section(header: Text(formatSectionHeader(date))) {
                    ForEach(groupedRides[date] ?? []) { ride in
                        RideRow(ride: ride)
                            .contentShape(Rectangle())
                            .onTapGesture {
                                selectedRide = ride  // 新增：点击打开详情
                            }
                    }
                }
            }
        }
        .listStyle(.insetGrouped)
    }
    
    // ... 其他代码
}

// NoRidesView 修正
struct NoRidesView: View {
    var body: some View {
        VStack(spacing: 20) {
            Spacer()
            
            Image(systemName: "bicycle")
                .font(.system(size: 60))
                .foregroundColor(.secondary)
            
            Text("No Rides Yet")
                .font(.title2)
                .fontWeight(.bold)
            
            Text("Start tracking your rides to see them here.")
                .font(.body)
                .foregroundColor(.secondary)
                .multilineTextAlignment(.center)
                .padding(.horizontal, 32)
            
            Spacer()
        }
    }
}
```

**Ride 模型扩展**:
```swift
// Ride.swift 添加 routePoints 计算属性
extension Ride {
    var routePoints: [RoutePoint]? {
        guard let routeData = routeData else { return nil }
        return try? JSONDecoder().decode([RoutePoint].self, from: routeData)
    }
}
```

**测试用例**:
1. 点击历史记录项正常打开详情页
2. 地图正确显示骑行路线
3. 统计数据正确显示
4. 起点/终点标记正确
5. 关联的自行车信息正确显示

**检验标准**:
- [ ] RideDetailView.swift 创建成功
- [ ] RideHistoryView 支持点击进入详情
- [ ] 详情页显示完整信息
- [ ] NoRidesView 参数修正
- [ ] 编译通过

---

## 四、P2 功能完善

### P2.1: 优化 Dashboard 数据刷新机制

**需求描述**:
当前 Dashboard 数据不会自动刷新，用户需要手动重新进入才能看到最新数据。

**实现方案**:
1. 使用 `@Query` 自动监听数据变化（已自动实现）
2. 添加下拉刷新功能
3. 添加最近骑行记录显示

**代码示例**:
```swift
// DashboardView.swift 优化
struct DashboardView: View {
    @Environment(\.modelContext) private var modelContext
    
    @Query(sort: \Bicycle.name) private var bicycles: [Bicycle]
    @Query(filter: #Predicate<Component> { $0.isActive }) private var activeComponents: [Component]
    @Query(filter: #Predicate<MaintenanceEvent> { !$0.isCompleted && $0.isScheduled }) private var pendingMaintenance: [MaintenanceEvent]
    @Query(sort: \Ride.startTime, order: .reverse) private var recentRides: [Ride]  // 新增
    
    @State private var showingAddBike = false
    
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(spacing: 20) {
                    if bicycles.isEmpty {
                        NoBikesView(onAddBike: { showingAddBike = true })
                    } else {
                        summarySection
                        bikesSection
                        componentsNeedingAttentionSection
                        upcomingMaintenanceSection
                        recentRidesSection  // 新增
                    }
                }
                .padding()
            }
            .refreshable {  // 新增：下拉刷新
                await refreshData()
            }
            .navigationTitle("Dashboard")
            .toolbar {
                ToolbarItem(placement: .topBarTrailing) {
                    Button(action: { showingAddBike = true }) {
                        Image(systemName: "plus")
                    }
                }
            }
            .sheet(isPresented: $showingAddBike) {
                AddBikeView()
            }
        }
    }
    
    private func refreshData() async {  // 新增
        // 触发维护检查
        await PersistenceController.shared.checkMaintenanceNeeds()
        
        // 保存上下文以确保数据同步
        try? modelContext.save()
    }
    
    // 新增：最近骑行记录 Section
    private var recentRidesSection: some View {
        VStack(alignment: .leading, spacing: 12) {
            SectionHeader(title: "Recent Rides", icon: "bicycle.circle")
            
            if let lastRide = recentRides.first {
                VStack(alignment: .leading, spacing: 8) {
                    HStack {
                        VStack(alignment: .leading) {
                            Text(lastRide.startTime.formatted(date: .abbreviated, time: .shortened))
                                .font(.subheadline)
                            if let bike = lastRide.bicycle {
                                Text(bike.name)
                                    .font(.caption)
                                    .foregroundColor(.secondary)
                            }
                        }
                        
                        Spacer()
                        
                        VStack(alignment: .trailing) {
                            Text(lastRide.distance.formattedDistance)
                                .font(.subheadline)
                                .fontWeight(.medium)
                            Text(lastRide.formattedDuration)
                                .font(.caption)
                                .foregroundColor(.secondary)
                        }
                    }
                }
                .padding()
                .background(Color(.systemGray6))
                .cornerRadius(Constants.UI.cornerRadius)
                .contentShape(Rectangle())
                .onTapGesture {
                    // 可以导航到骑行历史
                }
            } else {
                Text("No rides yet")
                    .foregroundColor(.secondary)
                    .frame(maxWidth: .infinity, alignment: .center)
                    .padding()
                    .background(Color(.systemGray6))
                    .cornerRadius(Constants.UI.cornerRadius)
            }
        }
    }
}
```

**测试用例**:
1. 下拉刷新正常触发
2. 数据变化自动更新UI
3. 最近骑行记录正确显示
4. 性能无明显下降

**检验标准**:
- [ ] 下拉刷新功能正常
- [ ] 最近骑行记录显示正确
- [ ] 数据自动更新
- [ ] 编译通过

---

## 五、P3 体验提升

### P3.1: 优化空状态视图

**需求描述**:
当前各页面的空状态视图不统一，需要统一设计提升用户体验。

**实现方案**:
1. 使用现有的 EmptyStateView 组件
2. 统一各页面的空状态设计
3. 修正 NoRidesView 的参数问题

**代码示例**:
```swift
// NoRidesView 修正（移除不存在的参数）
struct NoRidesView: View {
    var body: some View {
        EmptyStateView(
            icon: "bicycle",
            title: "No Rides Yet",
            description: "Start tracking your rides to see them here."
        )
    }
}
```

**测试用例**:
1. 空状态视图显示正确
2. 各页面空状态统一

**检验标准**:
- [ ] 空状态视图统一
- [ ] 编译通过

---

## 六、文件结构规范

### 6.1 优化后的文件结构

```
carebike/
├── App/
│   └── carebikeApp.swift
│
├── Core/
│   ├── Data/
│   │   ├── Models/
│   │   │   ├── Bicycle.swift
│   │   │   ├── Component.swift
│   │   │   ├── MaintenanceEvent.swift
│   │   │   ├── Ride.swift              # 添加 routePoints 计算属性
│   │   │   └── CostEntry.swift
│   │   └── Persistence/
│   │       └── PersistenceController.swift
│   │
│   ├── Domain/
│   │   └── Services/
│   │       ├── MaintenanceScheduler.swift
│   │       ├── CostAnalyzer.swift
│   │       └── NotificationManager.swift
│   │
│   └── Infrastructure/
│       ├── Location/
│       │   └── LocationManager.swift
│       └── Storage/
│           └── DocumentManager.swift
│
├── Features/
│   ├── Dashboard/
│   │   └── DashboardView.swift          # 优化：添加下拉刷新和最近骑行
│   │
│   ├── Bikes/
│   │   └── BikeListView.swift
│   │
│   ├── Tracking/
│   │   ├── StartRideView.swift          # 新增：独立模块
│   │   ├── ActiveRideView.swift         # 修改：添加 initialBike 参数
│   │   ├── RideHistoryView.swift        # 修改：添加详情页跳转
│   │   ├── RideDetailView.swift         # 新增：骑行详情页
│   │   └── ViewModels/
│   │       └── RideTrackingViewModel.swift  # 保留用于数据查询
│   │
│   └── ... 其他功能模块
│
├── Shared/
│   ├── Components/
│   │   ├── EmptyStateView.swift
│   │   └── ...
│   └── Utilities/
│       └── Constants.swift
│
└── ContentView.swift                    # 修改：移除内联 StartRideView
```

---

## 七、代码规范

### 7.1 Swift 代码规范

```swift
// MARK: - 常量定义
private let maxRetryCount = 3

// MARK: - 属性
@State private var isLoading = false

// MARK: - View Body
var body: some View {
    VStack {
        headerView
        contentView
    }
}

// MARK: - 子视图
private var headerView: some View {
    Text("Header")
}

// MARK: - 方法
private func loadData() async {
    // 方法实现
}
```

---

## 八、测试标准

### 8.1 功能测试清单

| 功能 | 测试项 | 预期结果 |
|------|--------|----------|
| StartRideView | 显示自行车选择器 | 正确显示所有自行车 |
| StartRideView | 空状态 | 无自行车时显示添加按钮 |
| StartRideView | 开始骑行 | 正确跳转到 ActiveRideView |
| ActiveRideView | 接收自行车参数 | 正确显示选择的自行车 |
| RideDetailView | 显示路线 | 地图正确显示骑行路线 |
| RideDetailView | 统计数据 | 距离、时长、速度正确 |
| Dashboard | 下拉刷新 | 触发维护检查 |
| Dashboard | 最近骑行 | 显示最近一次骑行 |

---

## 九、实施计划

### 9.1 实施顺序

| 阶段 | 任务 | 优先级 | 依赖 |
|------|------|--------|------|
| **P1** | 架构优化 | 🔴 高 | - |
| P1.1 | 重构 StartRideView | 🔴 高 | - |
| P1.2 | 添加 RideDetailView | 🔴 高 | P1.1 |
| **P2** | 功能完善 | 🟡 中 | P1 |
| P2.1 | 优化 Dashboard | 🟡 中 | - |
| **P3** | 体验提升 | 🟢 低 | P1, P2 |
| P3.1 | 优化空状态 | 🟢 低 | - |

### 9.2 废弃代码清理清单

| 文件 | 废弃代码 | 清理方式 |
|------|----------|----------|
| ContentView.swift | 内联 StartRideView | 删除内联代码，引用独立模块 |
| RideHistoryView.swift | NoRidesView(onStartRide:) | 修正为无参数版本 |

---

**文档结束**
